[project]
name = "editor-avo"
version = "0.0.1"
description = "Unofficial EditorAVO: AVO variation operator over EDL timelines"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[project.scripts]
editor-avo = "editor_avo.cli:main"

[tool.setuptools.packages.find]
include = ["editor_avo*"]
